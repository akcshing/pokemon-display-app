 version: 2
 jobs:
   build:
     docker:
       - image: 'circleci/python:2.7'
     steps:
       - checkout
       - run: npm build
       - aws-s3/sync:
          from: build
          to: 's3://terraform-alex-shing'
          overwrite: true
